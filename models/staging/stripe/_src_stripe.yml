sources:
  - name: stripe
    description: Clone of Stripe Database
    database: raw
    schema: stripe
    tables:
      - name: payment
        description: Raw payment data
        config:
          freshness:
            loaded_at_field: _batched_at
            warn_after: {count: 12, period: hour}
            error_after: {count: 24, period: hour}